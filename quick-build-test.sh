#!/bin/bash

# Скрипт для быстрой проверки сборки без полного Docker развертывания

echo "🚀 Быстрая проверка сборки..."
echo "================================"

# Установка зависимостей если нужно
if [ ! -d "node_modules" ]; then
    echo "📦 Установка зависимостей..."
    npm install
fi

# Генерация Prisma клиента
echo "🔧 Генерация Prisma клиента..."
npx prisma generate

# Проверка сборки
echo "🏗️  Запуск сборки..."
npm run build

if [ $? -eq 0 ]; then
    echo "✅ Сборка прошла успешно!"
    echo "💡 Теперь можно запускать полное развертывание"
else
    echo "❌ Сборка завершилась с ошибками"
    echo "🔧 Исправьте ошибки и попробуйте снова"
fi
